{
  "scripts": {
    "build": "npm run build --prefix codemirror && esbuild --bundle --sourcemap --format=esm --outdir=. gh-pages/editor.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@codemirror/commands": "^0.18.2",
    "@codemirror/gutter": "^0.18.2",
    "@defasm/codemirror": "file:codemirror",
    "@defasm/core": "file:core",
    "esbuild": "^0.11.18"
  }
}
